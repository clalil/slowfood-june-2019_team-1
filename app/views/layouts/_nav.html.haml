%nav
  %ul#navbar
    %li#lispace=link_to 'Home', root_path
    - if user_signed_in?
      %li#lispace=link_to "Hello, #{current_user.name}", "#"
      %li#lispace=link_to 'Logout', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure?' }
    - else 
      %li.active=link_to 'Login', new_user_session_path


    